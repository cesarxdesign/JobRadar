# The routing file is the decisions. It lives on the Desktop and never here.
RadarRouting.json
*.RadarRouting.json
# 36MB of JD text: the judge's evidence, rebuilt by every run. Not history.
data/jd.json
.DS_Store
__pycache__/
data/pool.partial.json
data/*.tmp

# His application history in test form (v1 applied roles, email-derived applications).
# Judge recall fixtures, never public. Files stay on disk, out of git.
data/test_applied.json
data/test_emails.json

# Email extraction: his inbox, local only
data/emails*
data/applied_hints.json
relay.html
data/applied_history.*

# His application history: 135 Gmail-proven applications and 185 routing
# decisions imported from v1 (2026-09-08). Judge fixtures, never public.
data/applications.json
data/history.json
data/marks.json

# fetcher's working cache: rebuilt from the boards, not history
data/originals.json
data/links.json
